import React, {useEffect, useState} from 'react';
import {
  StyleSheet,
  Text,
  TextInput,
  TouchableOpacity,
  View,
} from 'react-native';
import {styles} from './businessToolsCSS';

const BusinessScreen = ({navigation}) => {
  return (
    <View style={styles.min}>
      <Text>Welcom BusinessScreen</Text>
    </View>
  );
};

export default BusinessScreen;
