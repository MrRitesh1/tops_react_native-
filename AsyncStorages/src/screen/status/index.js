import React, {useEffect, useState} from 'react';
import {
  StyleSheet,
  Text,
  TextInput,
  TouchableOpacity,
  View,
} from 'react-native';

const StatusScreen = ({navigation}) => {
  return (
    <View style={[styles.min, styles.shado]}>
      <Text>Welcom StatusScreen</Text>
    </View>
  );
};
const styles = StyleSheet.create({});
export default StatusScreen;
