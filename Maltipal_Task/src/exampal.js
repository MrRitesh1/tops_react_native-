import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const ExampalScreen = () => {
  return (
    <View>
      <Text>Exampal Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ExampalScreen;
